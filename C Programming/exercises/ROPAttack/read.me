I think all the address end with 89 or 8b so you couldn't have them as a return address.
And I can't figure another way to bypass this. The program in working works because it writes internally the address so check it out.
